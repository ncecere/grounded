{
  "name": "@grounded/widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/widget.js",
  "scripts": {
    "dev": "vite",
    "build": "BUILD_TARGET=widget vite build && cp dist/widget.js ../../apps/web/public/widget.js && BUILD_TARGET=published-chat vite build && cp dist/published-chat.js ../../apps/web/public/published-chat.js",
    "build:widget": "BUILD_TARGET=widget vite build",
    "build:published-chat": "BUILD_TARGET=published-chat vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.28.2",
    "marked": "^17.0.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
